# タスク管理

このフォルダでは個別タスクを独立した markdown ファイルで管理します。
.design/tasks を正本として運用します。

## 命名規則

`YYYYMMDD-<優先度識別子>-title.md`

- 日付は作成日を使用
- 優先度識別子は `1` `2` `3A` `3B` `4` のように記述する
- title は英語のケバブケースで簡潔に記述
- 例: `20260227-1-fix-login.md` / `20260227-3A-update-api-spec.md` / `20260227-3B-add-api-tests.md`
- 同じ番号（例: `3A` と `3B`）は同一優先度で並行着手可能なタスクとして扱う

## テンプレート

```markdown
# [3A] タスクタイトル

## ステータス
- [ ] 未着手 / [x] 進行中 / [x] 完了

## owner
codex または claude-code

## 概要
[タスクの説明]

## 完了条件
- [ ] 条件1
- [ ] 条件2

## 備考
[必要に応じて追記]
```

## 使い方

### 新規タスクの作成
1. このフォルダに `YYYYMMDD-<優先度識別子>-title.md` 形式でファイルを作成
2. タスクタイトル先頭に同じ優先度識別子を付与する（例: `[3A]`）
3. `owner` を `codex` または `claude-code` で明記
4. 上記テンプレートに従って内容を記述
5. 必要に応じて外部ツールへ同内容を転記する

### タスクの確認
`/tasks` コマンドでこのフォルダ内のタスクを一覧・確認できます。

### 完了したタスク
完了したタスクファイルは削除するか、`completed/` サブフォルダに移動してアーカイブできます。
